# HR Frames Frontend

This is a fully static frontend application for HR Frames.
No backend, APIs, or Cloudinary dependencies exist.
Images are served directly from `src/assets/images`.
Data is static and served from `src/data`.

## Setup

```bash
npm install
npm run dev
```
